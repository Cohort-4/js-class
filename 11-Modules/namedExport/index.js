const addition = (x, y) => {
  return x + y;
};

const substract = (p, r) => p - r;

function multiply(m, n) {
  return m * n;
}

const division = function (a, b) {
  return a / b;
};

export { addition, substract, multiply, division };
