const addition = (x, y) => {
  return x + y;
};

const substraction = (p, r) => p - r;

function multiplication(m, n) {
  return m * n;
}

const division = function (a, b) {
  return a / b;
};

export {
  addition as add,
  substraction as substract,
  multiplication as multiply,
  division as divide,
};
