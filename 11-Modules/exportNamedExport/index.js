export const addition = (x, y) => {
  return x + y;
};

export const substract = (p, r) => p - r;

export function multiply(m, n) {
  return m * n;
}

export const division = function (a, b) {
  return a / b;
};
